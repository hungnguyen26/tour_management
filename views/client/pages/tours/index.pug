extends ../../layout/default.pug
include ../../mixins/box-head.pug
block main
    .containner.my-3 
        .row    
            .col-12 
                +box-head(pageTitle)
        
        ul 
            each item in tours 
                li #{item.title}